node_modules/
dist/
.DS_Store
*.local
.env
.env.*

# Artefactos temporales que a veces deja Vite al cargar la config (ESM interop)
vite.config.*.timestamp-*.mjs

# Log local de cada corrida de scripts/run-snapshot-and-push.sh (no se commitea)
scripts/snapshot-historico.log
